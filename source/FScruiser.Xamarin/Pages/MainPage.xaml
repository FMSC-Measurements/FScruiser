<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FScruiser.Pages.MainPage"
             xmlns:cm="clr-namespace:Caliburn.Micro.Xamarin.Forms;assembly=Caliburn.Micro.Platform.Xamarin.Forms"
             x:Name="TopPage">
    <StackLayout>
        <Button Command="{Binding OpenFileCommand}">Open File</Button>
        <Label>Cutting Units:</Label>
        <ScrollView>
            <ListView x:Name="UnitListView" ItemsSource="{Binding Units}" ItemSelected="UnitListView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <StackLayout Orientation="Horizontal">
                                    <Label>Cutting Unit:</Label>
                                    <Label Text="{Binding Code}"></Label>
                                </StackLayout>
                                <StackLayout Orientation="Horizontal">
                                    <Label>Strata:</Label>
                                    <Label Text="{Binding StrataNames}"></Label>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
    </StackLayout>
</ContentPage>