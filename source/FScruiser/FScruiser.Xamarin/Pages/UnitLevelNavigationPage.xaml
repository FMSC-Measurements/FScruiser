<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FScruiser.Pages.UnitLevelNavigationPage"
             x:Name="Self"
             Title="Strata"
             Icon="ic_menu_white_36dp.png">
    <StackLayout>
        <ListView x:Name="ListView"
                  ItemsSource="{Binding Strata}"
                  VerticalOptions="StartAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding StratumCode}"
                              Detail="{Binding CruiseMethod}"
                          Command="{Binding BindingContext.ShowTalliesCommand, Source={x:Reference Self}}"
                          CommandParameter="{Binding .}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackLayout Orientation="Horizontal">
            <Button Text="Trees"
                    Command="{Binding ShowTreesCommand}" />
        </StackLayout>
    </StackLayout>
</ContentPage>