<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Cells="clr-namespace:FScruiser.Cells"
             x:Class="FScruiser.Pages.StratumTalliesPage"
             x:Name="Page">
    <StackLayout>
        <ScrollView>
            <ListView ItemsSource="{Binding Counts}"
                      HasUnevenRows="True"
                      ItemSelected="ListView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Cells:TallyCell Command="{Binding Path=BindingContext.TallyCommand, Source={x:Reference Page}}"
                                 CommandParameter="{Binding .}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
        <StackLayout Orientation="Horizontal">
            <Button Text="Trees" HorizontalOptions="Start" />
            <Button Text="TallyHistory" HorizontalOptions="End" />
        </StackLayout>
    </StackLayout>
</ContentPage>