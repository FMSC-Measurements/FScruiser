<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:FScruiser.Pages"
             x:Class="FScruiser.Pages.CruisePage"
             Title="Cutting Units"
             x:Name="TopPage">
    <StackLayout>
        <Label Text="Cutting Units" FontSize="Large" HorizontalOptions="Center" />
        <ListView
            x:Name="CuttingUnitList"
            ItemsSource ="{Binding CuttingUnits}"
            ItemSelected="ListView_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding CuttingUnitCode}"
                        Command="{Binding BindingContext.ShowDataEntryCommand, Source={x:Reference TopPage}}"
                        CommandParameter="{Binding .}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>