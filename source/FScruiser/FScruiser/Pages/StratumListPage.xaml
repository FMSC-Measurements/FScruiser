<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FScruiser.Pages.StratumListPage">
    <ListView x:Name="ListView" ItemsSource="{Binding Strata}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding StratumCode}"
                          Command="{Binding BindingContext.ShowTalliesCommand Source={x:Reference ListView}}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>