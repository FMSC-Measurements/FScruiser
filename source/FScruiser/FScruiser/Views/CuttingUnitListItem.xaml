<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FScruiser.Views.CuttingUnitListItem">
    <ViewCell.ContextActions>
        <MenuItem Text="Data Entry" />
    </ViewCell.ContextActions>
    <StackLayout Orientation="Horizontal">
        <Label Text ="{Binding CuttingUnitCode}" HorizontalOptions="FillAndExpand" />
        <Button
            Text = "Data Entry"
            Command="{Binding ShowDataEntryCommand}"
            CommandParameter="{Binding .}" />
    </StackLayout>
    <ListView
        ItemsSource="{Binding Strata}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell
                    Text="{Binding StratumCode}"
                    Detail="{Binding Method}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ViewCell>